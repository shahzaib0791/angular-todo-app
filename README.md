
# TodoApp

A simple Todo application built with Angular.

## Features
- Add, edit, and delete tasks
- Mark tasks as complete/incomplete
- Responsive design for various screen sizes

## Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/shahzaib0791/angular-todo-app.git
   ```
2. Navigate to the project directory:
   ```bash
   cd angular-todo-app
   ```
3. Install dependencies:
   ```bash
   npm install
   ```

## Development Server
Run the server:
```bash
ng serve
```
Navigate to `http://localhost:4200/`.

## Commands
- **Generate Components**:  
  `ng generate component [component-name]`
- **Build Application**:  
  `ng build`  
  Output is in the `dist/` directory.
- **Unit Tests**:  
  Run `ng test` using Karma.
- **E2E Tests**:  
  Run `ng e2e` after adding an end-to-end testing package.

## Contribution
Contributions are welcome! Please fork the repository and create a pull request with changes.

## License
[MIT](LICENSE)
